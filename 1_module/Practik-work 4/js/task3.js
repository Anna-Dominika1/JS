let name =  prompt("Введіть ім'я яке буде містити більше 4 символів");

let syrname = prompt("Введіть прізвище яке буде містити більше 5 символів");

let syrnameName = name + syrname;

if (name.length > 4);

 if  (syrname.length > 5);
 
if  (syrnameName.length){
    console.log (syrnameName.length)
 }              
 else {
    console.log(`УПС`);
 }